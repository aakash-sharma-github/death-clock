/** @type {import('next').NextConfig} */
const nextConfig = {
   // output: "export", // Exports as static files
   // distDir: "death-clock-extension", // Custom build directory
};

export default nextConfig;
